## Сборка

```bash
g++ main.cpp -O2 -std=c++17 -o app.exe
```

## Запуск

```bash
app.exe
```

Программа читает входные данные из файла `input.json` и записывает результат в `output.json`.

Файлы `input.json`, `output.json` и `app.exe` должны находиться в одной папке.
